# GitHub Token (optional) - Used to avoid rate limits when fetching releases
GH_TOKEN=

# Use mock data for releases instead of fetching from GitHub API
# Set to 'true' to enable mock data for development/preview
USE_MOCK_DATA=true
